Decode1 dec;

float coeff[4][4];

for(int i; i < coeff.size(); i++)
{
    for(int j; j < coeff[0].size(); j++)
    {
        Math.random2f(-1.0,1.0) => coeff[i][j];
        cherr <= coeff[i][j] <= " ";
    }
    cherr <= IO.newline();
}

dec.placement(coeff);